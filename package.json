{
  "name": "gql-server-example",
  "version": "0.1.0",
  "main": "./src/index.ts",
  "license": "MIT",
  "scripts": {
    "start": "yarn build:live",
    "build": "tsc -p .",
    "build:live": "nodemon --watch 'src' -e ts,gql --exec 'ts-node' src/index.ts"
  },
  "devDependencies": {
    "@types/node": "^14.14.14",
    "nodemon": "^2.0.6",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "apollo-datasource-rest": "^0.9.5",
    "apollo-server": "^2.19.0",
    "graphql": "^15.4.0",
    "graphql-import": "^1.0.2"
  }
}
